# Variable de entorno para la función serverless /api/analyze (Vercel)
# Solo es necesaria si quieres el análisis con IA (Claude).
# Sin ella, la app usa el analizador léxico local y funciona igual.
ANTHROPIC_API_KEY=sk-ant-...
